import { test } from 'vitest';

test('successfully renders table on 200 request', async () => {
  // render(<RepresentativesTable />);
  // expect(screen.findByText('Representatives')).toBeDefined();
  // // user 1
  // expect(screen.findByText('John Johnson')).toBeDefined();
  // // user 2
  // expect(screen.findByText('Mike Mickelson')).toBeDefined();
  // // user 3
  // expect(screen.findByText('Jack Jackson')).toBeDefined();
  // // user 4
  // expect(screen.findByText('Robert Robertson')).toBeDefined();
  // // user 5
  // expect(screen.findByText('Connor Connorson')).toBeDefined();
  // // user 6
  // expect(screen.findByText('Kyle Kyleson')).toBeDefined();
});
